runtime: python311
service: rca-engine

automatic_scaling:
  max_instances: 10

entrypoint: gunicorn -w 2 -k uvicorn.workers.UvicornWorker api.main:app

env_variables:
  APP_NAME: "RCA Engine"
  DEBUG: "false"
  API_PREFIX: "/api/v1"
