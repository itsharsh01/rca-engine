from sqlalchemy.ext.asyncio import AsyncSession

async def seed_default_org(session: AsyncSession) -> None:
    """
    Populate a default organization, project, and application record.
    """
    pass
